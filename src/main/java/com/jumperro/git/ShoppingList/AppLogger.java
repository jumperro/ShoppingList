package com.jumperro.git.ShoppingList;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public interface AppLogger {
    Logger logger = LoggerFactory.getLogger(ShoppingListApplication.class);
}
